import { PostsAdminView } from '@/views';
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Admin - Posts',
  description: 'Posts admin page of creative thoughts agency'
};

export default PostsAdminView;
