import { AboutView } from '@/views';
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'About',
  description: 'About page of creative thoughts agency'
};

export default AboutView;
