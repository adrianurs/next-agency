import { Loader, ViewContainer } from '@/components';
import { FC } from 'react';

const Loading: FC = () => {
  return (
    <ViewContainer>
      <Loader />
    </ViewContainer>
  );
};

export default Loading;
