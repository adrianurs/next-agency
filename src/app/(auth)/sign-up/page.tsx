import { SignUpView } from '@/views';
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Sign Up',
  description: 'Sign Up page of creative thoughts agency'
};

export default SignUpView;
