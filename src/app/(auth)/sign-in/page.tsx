import { SignInView } from '@/views';
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Sign In',
  description: 'Sign In page of creative thoughts agency'
};

export default SignInView;
