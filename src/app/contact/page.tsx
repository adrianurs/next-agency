import { ContactView } from '@/views';
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Contact',
  description: 'Contact page of creative thoughts agency'
};

export default ContactView;
